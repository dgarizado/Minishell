IN LEXIC:

-- SPLIT IN WORDS --

1. g_data.token1: This is the first split of the program. After checking if there are unclosed quotes, an array of strings is generated via a split by spaces that include all characters. The input is splitted in words. 

-- EXPAND --
1. 

-- UNTOKEN --

1. g_data.input_ex: After doing the expansions, all words are "unsplit" into a new string. This facilitates the analysis for determining the number of pipes to be needed. 

-- REDICS ---

1. Redics analysis is done always inside a child.